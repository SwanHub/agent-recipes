# People routes

App pages and API handlers.

## Do

- `/profiles/all`, `/profile/[slug]` — placeholder for recipes section (step 5)
- `GET /api/people`, `GET /api/people/[segment]` — omit if pages pass props only

## Verify

- `cd output && npm run build` passes.
