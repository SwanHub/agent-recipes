# Static stubs

Placeholder routes that must exist before later phases.

## Do

1. `output/app/join/page.tsx` — minimal onboarding stub, on-brand typography.
2. `output/app/not-found.tsx` — friendly 404 with a link back to `/`.

## Verify

- `/join` and a non-existent path render and feel consistent with the chrome from the sibling step.
