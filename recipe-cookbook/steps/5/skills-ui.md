# Skills UI

Components only. Recipes reuse the file-tree preview in P7.

## Do

- Skill preview card, searchable catalog, `profile-skills-section`
- GitHub file-tree preview, markdown preview, detail header actions (`kind: "recipe" | "skill"`) — see `CONVENTIONS.md` for CTA rules

## Verify

- `cd output && npm run build` passes.
