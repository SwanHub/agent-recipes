# Profile — skills section

Wire skills into the profile page left as a placeholder in P3.

## Do

In `output/app/profile/[slug]/page.tsx`: render `ProfileSkillsSection` below bio/links. Optional mono badge for this person's skill install total.

## Verify

- `cd output && npm run build` passes. Visual check at P6 USER PAUSE.
