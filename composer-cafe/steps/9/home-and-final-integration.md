# Home + final integration

Landing page composes live people, recipes, and skills.

## Do

- Featured fetches + API routes: first N recipes/skills by title (see `CONVENTIONS.md` — no hardcoded slug lists).
- Landing components: hero heading, section heading, join CTA pill.
- Replace `output/app/page.tsx` — parallel fetch, sections: hero, featured recipes, featured skills, community (first 6), join.

## Verify — USER PAUSE

> Final walkthrough: `/`, `/recipes/all`, `/skills/all`, a detail page each, `/profiles/all` → profile, `/join`, a 404. Sign off when ready.

**Wait for explicit approval.** Recipe complete.

## Done

Summarize routes, seeded counts, `output/` location, Vercel deploy notes (env vars, schema already applied).
