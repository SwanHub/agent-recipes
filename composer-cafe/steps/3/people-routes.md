# People routes

App pages and API handlers.

## Do

- `/profiles/all`, `/profile/[slug]` — placeholders for skills (P5) and recipes (P7)
- `GET /api/catalog/people`, `GET /api/catalog/people/[segment]` — omit if pages pass props only

## Verify

- `cd output && npm run build` passes.
