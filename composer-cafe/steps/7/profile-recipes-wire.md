# Profile — recipes section

Complete the profile page catalog areas.

## Do

In `output/app/profile/[slug]/page.tsx`: add `ProfileRecipesSection`. Update install badge to sum recipes + skills for this person.

## Verify

- `cd output && npm run build` passes. Visual check at P8 USER PAUSE.
